
  var typed = new Typed('#element', {
    strings: ['Web designing', 'Web development', 'Application development', 'Video editing'],
    typeSpeed: 80,
    backSpeed: 50,        // Speed of backspacing
    backDelay: 1000,      // Delay before backspacing starts
    startDelay: 500,      // Delay before typing starts
    loop: true,           // Loop the typing animation
    smartBackspace: true, // Only backspace what’s necessary
    
    
  });
    var typed = new Typed("#text_work", {
    strings: ["Work"],
    typeSpeed: 150,
    backspeed:200,
    loop: true
  });


